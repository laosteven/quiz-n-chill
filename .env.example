# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration
# For production, set to your domain(s), e.g., https://yourdomain.com
# For development, you can use * or leave empty to allow all origins
CORS_ORIGIN=*
